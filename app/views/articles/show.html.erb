<h2><%= markdown @article.title %></h2>

  <small>
    submitted <%= time_ago_in_words(@article.created_at) %> ago by <%= @article.user.name %><br/>
  </small>

<h3><%= markdown @article.body %></h3>

  <%= link_to "Edit", edit_article_path(@article), class: 'btn btn-danger' %>

  <small>
    edited <%= time_ago_in_words(@article.created_at) %> ago by <%= @article.user.name %><br/>
  </small>


